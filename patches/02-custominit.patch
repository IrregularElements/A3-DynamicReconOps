---
 src/init.sqf            |    4 ++++
 src/initPlayerLocal.sqf |    5 +++++
 src/initServer.sqf      |    5 +++++
 3 files changed, 14 insertions(+)

--- a/src/initServer.sqf
+++ b/src/initServer.sqf
@@ -19,6 +19,11 @@
 missionNameSpace setVariable ["aoLocation", "", true];
 missionNameSpace setVariable ["lobbyComplete", 0, true];
 
+_customInitServer = "customInitServer.sqf";
+if (not ((loadFile _customInitServer) isEqualTo "")) then {
+	[] execVM _customInitServer;
+};
+
 [] execVM "start.sqf";
 
 
--- a/src/initPlayerLocal.sqf
+++ b/src/initPlayerLocal.sqf
@@ -369,6 +369,11 @@
 	Thank you for playing!	
 "]];
 
+_customInitPlayerLocal = "customInitPlayerLocal.sqf";
+if (not ((loadFile _customInitPlayerLocal) isEqualTo "")) then {
+	_this execVM _customInitPlayerLocal;
+};
+
 // Start saving player loadout periodically
 [] spawn {
 	loadoutSavingStarted = true;
--- /dev/null
+++ b/src/init.sqf
@@ -0,0 +1,4 @@
+_customInitPlayerLocal = "customInit.sqf";
+if (not ((loadFile _customInit) isEqualTo "")) then {
+	[] execVM _customInit;
+};
